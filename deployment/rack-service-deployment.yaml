apiVersion: apps/v1
kind: Deployment
metadata:
  name: rack-service-deployment
  labels:
    app: rack-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rack-service
  template:
    metadata:
      labels:
        app: rack-service
    spec:
      containers:
        - name: rack-service
          image: tgustafsson/rack-service:latest
          ports:
            - containerPort: 8080
          env:
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: db-username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: db-password