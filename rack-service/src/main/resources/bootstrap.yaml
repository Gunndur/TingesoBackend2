spring:
  application:
    name: rack-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/bdRack
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: create
